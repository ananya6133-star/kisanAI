{
  "name": "ai-agriculture-crop-advisory-assistant",
  "version": "1.0.0",
  "private": true,
  "description": "Production-grade AI-Powered Agriculture Crop Advisory Assistant with React, Node.js, Supabase, and Google Gemini",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "build": "npm install && cd server && npm install && cd ../client && npm install && npm run build",
    "start": "cd server && npm start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
